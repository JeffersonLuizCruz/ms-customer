spring:  application:    name: mscustomers#  profiles:#    active:#    - local  datasource:    url: jdbc:h2:mem:testdb    driver-class-name: org.h2.Driver    username: sa    password:   jpa:    database-platform: org.hibernate.dialect.H2Dialect    hibernate:      ddl-auto: create  h2:    console:      enabled: true      path: /h2 #http://localhost:64191/h2server:  port: 0#eureka:#  client:#    service-url:#      defaultZone: http://eureka:eureka@ms-customer:8761/eureka#  instance:#    instance-id: ${spring.application.name}:${spring.application.instance_id:${random.value}} eureka:  client:    service-url:      defaultZone: http://eureka:eureka@${EUREKA_SERVER}:8761/eureka  instance:    instance-id: ${spring.application.name}:${spring.application.instance_id:${random.value}}